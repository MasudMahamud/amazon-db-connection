import React from 'react';
import ProductCard from '../ProductCard/ProductCard';


const Product = (props) => {
    
    return (
        <>
            <ProductCard props={props}></ProductCard>
        </>
    );
};

export default Product;